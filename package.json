{
  "name": "neolab.ai",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "pnpm@10.34.5",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --filter @neolab/web dev",
    "build": "pnpm content:build && pnpm typecheck && pnpm test && pnpm --filter @neolab/web build",
    "typecheck": "pnpm -r --parallel run typecheck",
    "lint": "eslint . && prettier --check .",
    "format": "prettier --write .",
    "test": "vitest run",
    "test:browser": "echo 'not implemented, see implementation-plan Stage 5' && exit 1",
    "test:e2e": "pnpm --filter @neolab/web test:e2e",
    "content:build": "pnpm --filter @neolab/content-compiler run compile",
    "content:check": "pnpm --filter @neolab/content-compiler run compile",
    "balance:smoke": "echo 'not implemented, see implementation-plan Stage 6' && exit 1",
    "balance:full": "echo 'not implemented, see implementation-plan Stage 6' && exit 1",
    "save:inspect": "echo 'not implemented, see implementation-plan Stage 5' && exit 1"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "prettier": "catalog:",
    "typescript": "catalog:",
    "typescript-eslint": "catalog:",
    "vitest": "catalog:"
  }
}
